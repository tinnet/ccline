version: 2
updates:
  - package-ecosystem: cargo
    directory: /
    schedule:
      interval: weekly
  # No github-actions ecosystem — release.yml is autogenerated by cargo-dist.
  # Update it by bumping cargo-dist-version in dist-workspace.toml and running `dist generate`.
